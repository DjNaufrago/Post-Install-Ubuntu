The message: System program problem detected, is always displayed after each reboot despite reporting the problem. This is because the reports still remain in the directory where they are stored.

To remove them, type:  
`sudo rm /var/crash/*`
